# Pinned to the deployed versions so image rebuilds are reproducible and don't
# silently drift onto a new major (which is what broke /api connectivity: under
# a Starlette upgrade the collection-root redirect normalisation in app/main.py
# stopped taking effect, so /api/* 307-redirected to the internal backend host).
# Starlette is pinned explicitly because it is the ASGI core that regressed.
fastapi==0.138.0
starlette==1.3.1
uvicorn[standard]==0.49.0
sqlalchemy>=2.0.51
greenlet>=3.5.2
asyncpg
clickhouse-connect[async]>=1.3.0,<2
pydantic[email]>=2.13.4
pydantic-settings>=2.14.2
python-jose[cryptography]
passlib[bcrypt]==1.7.4
# pin bcrypt to avoid incompatibilities with passlib
bcrypt==3.2.0
python-multipart
python-dotenv
httpx
pyfaidx
pysam
PyYAML
# Clinical CNV knowledgebase build script (scripts/clinical_cnv_knowledgebase.py),
# invoked as a subprocess by the admin "rebuild knowledgebase" action.
pandas
requests
beautifulsoup4
lxml
intervaltree
tqdm
# AWS S3 access for production storage backend (STORAGE_BACKEND=s3): presigned URLs
# for IGV alignments and staging of family-package sources.
boto3
